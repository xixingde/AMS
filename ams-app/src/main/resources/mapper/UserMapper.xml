<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cmhk.ams.mapper.UserMapper">
    <select id="selectUserVOList" resultType="com.cmhk.ams.model.vo.UserVO">
        SELECT u.id, u.username, r.role_name as roleName, r.role_code as roleCode, u.created_at
        FROM users u
        LEFT JOIN roles r ON u.role_id = r.id
        WHERE u.deleted_at IS NULL
        ORDER BY u.created_at DESC
    </select>
</mapper>
